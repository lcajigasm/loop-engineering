---
description: Watch a goal for a bounded time and re-run its gate after local commits touch its scope; never edit code.
argument-hint: <goal-id> [minutes]
---
Run the loop-engineering `watch` command.

Follow the canonical spec's `watch` section exactly, including the Codex
capability notes: read `~/.agents/skills/loop-engineering/core/COMMANDS.md`
(or, if this project has its own copy,
`.agents/skills/loop-engineering/core/COMMANDS.md`). Converse in the user's
language; write files in English.

Arguments: $ARGUMENTS
